from src.spt_track_duration import get_spotify_duration
from src.yt_track_duration import get_youtube_duration

def get_story_length(track_duration: int, wpm: int = 178) -> int:
    
    words = (wpm / 60) * track_duration_sec  # Words based on WPM and duration
    nb_of_characters = words * 5  # Approximate nb_of_characters (average word length)
    return int(nb_of_characters)

# def youtube_story_length(video_duration: int, wpm: int = 178) -> int:
#     words = (wpm / 60) * video_duration_sec  # Words based on WPM and duration
#     nb_of_characters = words * 5  # Approximate nb_of_characters (average word length)
    return int(nb_of_characters)

# Example usage:
# track_duration_sec = 180  # 3 minutes
# estimated_chars = estimate_story_length(track_duration_sec)
# print(f"Estimated story length: {estimated_chars} nb_of_characters")
